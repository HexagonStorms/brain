# Machine-local files don't live in this repo at all — they stay in ~/.claude/
# and are never moved here. So this .gitignore only needs to keep editor and
# OS noise out.

.DS_Store
Thumbs.db
*.swp
*.swo
*~
